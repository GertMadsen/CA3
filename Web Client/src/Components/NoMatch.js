import React, { Component } from "react"

const NoMatch = () => (
    <div> 
        <h1>There is no page matching the URL requested.</h1>
        <h3>Please enter a valid URL to proceed.</h3>
    </div>
)

export default NoMatch;